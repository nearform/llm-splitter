## 1. Pin the failures before changing anything

- [x] 1.1 Add the real-tokenizer regression from [design.md](./design.md) → "Acceptance criteria" § 1 to `test/split.test.js`, and confirm it **fails** at head with `Splitter returned a part that could not be located in input`.
- [x] 1.2 Add the splitter-only synthetic from "Acceptance criteria" § 2 and confirm it also fails at head. Two independent repros, so the regression survives the `tiktoken` devDependency being dropped.
- [x] 1.3 Add the tier 3 offset regression. **Corrected during apply:** "Acceptance criteria" § 3 specified `"a �b"` → `[["a ", 0, 2], ["�b", 2, 4]]`, which is byte-identical to the existing test "locates a replacement char that is genuinely in the source" and passes both before and after — a duplicate, not a regression. Replaced with a `k = 2` shape: `split("abcd", { splitter: () => ["��cd"] })`, head `[["cd", 2, 4]]` → fixed `[["abcd", 0, 4]]`. It fails at head. **Corrected after review:** the claim that two leading replacement chars make it catch a naive `cursor + 1` was wrong — "c" is found at index 2 from any of `cursor`, `cursor + 1`, or `cursor + 2`, so this test pins only the `- anchor.offset` subtraction. The `cursor + anchor.offset` search start had no coverage at all (the full suite stayed green with it removed) until "searches for the anchor grapheme forward of the part's left edge" was added.
- [x] 1.4 Record the baseline control result in the test comment: `"漢 hello world X tail"` succeeds with `[{ text: " hello world X tail", start: 1, end: 20 }]`, which is what makes the U+FFFD the isolated cause rather than the multi-byte character.

## 2. Apply the fix — both halves, in this order

- [x] 2.1 In `anchorParts` ([src/split.js](../../../../src/split.js)), make `firstAnchorGrapheme` return `{ segment, offset }` (the offset is `Intl.Segmenter`'s `index`, already available) and change the tier 3 locate to `indexOf(anchor.segment, cursor + anchor.offset) - anchor.offset`. Update the JSDoc `@returns`; `check:types` passes with the object form.
- [x] 2.2 Confirm 2.1 alone changes exactly one unit expectation — "drops unanchorable parts without dropping mixed ones" goes from `[["cd", 2, 4]]` to `[["bcd", 1, 4]]` — and update it per design.md → Decision 3, with a comment saying why 1 is the correct left edge. Measured counts: § 1 brings the suite to **170** tests, of which all three new ones fail at head → 167 pass. 2.1 fixes 1.3 and breaks the expectation above → 167 pass again. Updating it here → 168 pass, with 1.1 and 1.2 still failing until 2.3.
- [x] 2.3 Replace the tier 2 guard's `sourceHasReplacement || !splitPart.includes(REPLACEMENT_CHAR)` with the bare `!splitPart.includes(REPLACEMENT_CHAR)`, and delete the now-unused `sourceHasReplacement` probe.
- [x] 2.4 Comment the tier 3 call site with _why_ the offset subtraction is load-bearing: it is what makes 2.3 safe. Design.md → Decision 2 records that dropping the disjunct **without** 2.1 fails "locates a replacement char that is genuinely in the source" (`[["a �",0,3],["b",3,4]]` instead of `[["a ",0,2],["�b",2,4]]`) and the contract fuzz. Without this comment the two edits look independent and either can be reverted alone — which is worse than reverting both.
- [x] 2.5 Confirm both § 1 regressions now pass and `npm test` is **170/170**. Task 3.4 adds one more test, taking the final count to 171.

## 3. Verify against the measurements, not just the suite

- [x] 3.1 Run the harness in design.md → "Reproducing the measurements" against an unpatched and a patched copy of `src/`. It covers 3.1-3.3 in one invocation and exits non-zero on any failed gate, so there is no harness to rewrite from prose. Gate: **zero `OK→THROW`** and **zero invariant violations** across all three sweeps (3,000 strings, 10,000 strings, 2,000 arrays).
- [x] 3.2 Recorded. Harness run from the repo root against `src/` at `HEAD` (unpatched) and the working tree (patched): baseline threw **743 / 2,487 / 472** across the 3,000-string, 10,000-string and 2,000-array sweeps; patched threw **0 / 0 / 0**, with **0 `OK→THROW`** and **0 invariant violations** on all three. Positions moved 142 / 461 / 107. Identical to the spike numbers, so the implementation matches what was measured.
- [x] 3.3 Oracle: baseline threw 1,534 / off-boundary 4; patched threw 0 / off-boundary 7. **0 `ok→off`** regressions, and all 7 residuals carry the tier 1 signature (the harness asserts each lands on a literal U+FFFD) — 0 unexplained. The 7-vs-4 comparison is not a regression: baseline only got to attempt 466 of the 2,000 cases.
- [x] 3.4 Added "grows linearly when the source itself contains U+FFFD" beside the existing scaling test, with mixed U+FFFD parts (`BASE` 12,500, `SPAN` 8, threshold `SPAN * 2`). Suite is **171/171**. Verified it bites: on unpatched `src/` it fails at **42.4x**, while the pre-existing clean-source test still passes there. Growth measured 7.6-8.2x patched across repeated runs, baseline denominator 10.2-12.3ms; costs ~157ms of suite time.
- [x] 3.5 Spot-checked the 461 moved cases against ground truth — byte arithmetic for `tiktoken`, exact-offset arithmetic for `space`. Restricted to `space` + `character` strategy (220 cases), where offset arithmetic is valid: **both sides fully on truth in all 220**. The positions differ over which parts are _dropped_ (manufactured bare U+FFFD), not over where surviving parts land — matching the neutrality sweep's "omitted, never moved". A first pass that also included `paragraph` strategy reported 5 apparent patched-side errors; those were the diagnostic's fault, not the fix's — in paragraph mode positions are group-relative, so whole-text offset arithmetic is the wrong truth function.
- [x] 3.6 tiktoken multibyte fixtures pass in-suite, Devanagari `"Hindi: नमस्ते दुनिया"` included (test/split.test.js:1182). Compared directly against baseline: that string and six other multibyte fixtures produce **byte-identical positions**, so the change is inert on clean multibyte input. Adding a literal U+FFFD to the Devanagari string throws on baseline and anchors cleanly patched. gte-small remains out of scope, owned by `tokenizer-length-inflation` task 4.6.

## 4. Reconcile the spec delta with what actually shipped

- [x] 4.1 [specs/multibyte-anchoring/spec.md](./specs/multibyte-anchoring/spec.md) is written for the measured behavior, including both residuals as stated exceptions. Confirm each still matches: the tier 1 exception (Decision 4), the decoy exception (Risks), and the removal of the U+FFFD linearity carve-out. If any measurement in task 3 came out differently, the delta moves — not the gate.
- [x] 4.2 Two shipped scenarios have their outcome **inverted**: "Tier 2 attempted when the source itself contains U+FFFD" and "Source containing U+FFFD keeps the unbounded search (known limitation)". Both were written by `anchor-scan-short-circuit` as permanent facts about the problem; they were consequences of the disjunct. The delta format cannot drop a scenario from a MODIFIED requirement (`validate --strict` rejects it, and REMOVED/RENAMED do not reset the scenario set either), so both are retained with corrected bodies and now-stale titles.
- [x] 4.3 Run `openspec validate --all --strict` and fix any delta that fails.

## 5. Close the user-facing overclaim

- [x] 5.1 Update README's "Supported tokenizers (and a known limitation)" section: the `tiktoken` line currently reads "✅ … substitutes exactly one U+FFFD per undecodable byte, so length matches source span" with normalizing tokenizers as the only caveat. State the literal-U+FFFD-in-source behavior including both exceptions.
- [x] 5.2 Rewrite AGENTS.md's algorithm-map bullet "Tier 2 is skipped when it provably cannot match". After 2.3 the skip is no longer provable-equivalence only — it is equivalence for a clean source and a deliberate correctness preference for a U+FFFD-bearing one. Add the tier 3 left-edge rule next to it, and note that the two edits are a pair.
- [x] 5.3 Update AGENTS.md's "Quadratic tier-2 anchor scan" entry under "Open work / future": that change's carve-out for a U+FFFD-bearing source is now closed, with the measurement (40.8x → 10.7x cost for an 8x span).
- [x] 5.4 Add a changeset (`npx changeset`). Positions and chunk counts change for affected inputs, so this is **not** a patch — it needs the same "regenerate persisted embeddings / citation offsets" note the `0.3.0` rewrite carries.

## 6. Hand off what is not done

- [x] 6.1 Update `tokenizer-length-inflation`'s `research.md` — it recorded this shape as an open "fourth instance of the same root cause". Done: it now names the two-edit fix and the tier 1 residual, and states the two live consequences for that change (its Phase 1 evidence rows predate the tier 3 fix; its identity path now carries that fix).
- [x] 6.2 Hand the gte-small verification to `tokenizer-length-inflation`. Done: its task 4.6, scoped to the corrected tier 3 on the identity path, plus its task 3.4 amended so "keep tiers 1-2 identical" means identical to `main` at rebase time rather than to what that change was written against.
- [x] 6.3 Recorded the backtracking anchor walk in AGENTS.md → "Open work / future" as its own entry, including the undecidability demonstration and the instruction not to accept a proposed local guard that ignores it. It needed to outlive this change's archive, so AGENTS.md rather than only design.md → Open Questions.
- [x] 6.4 Done post-archive. Renamed "Tier 2 attempted when the source itself contains U+FFFD" to "Tier 2 skipped even when the source itself contains U+FFFD", and deleted "Source containing U+FFFD keeps the unbounded search (known limitation)" outright — "Growing a U+FFFD-bearing input grows anchoring time proportionally" already covers it. Also stripped the "this change inverts…" / "prior to this change" narration the delta carried into `openspec/specs/multibyte-anchoring/spec.md`: a living contract states what holds, not what it used to say.
- [x] 6.5 Archived as `2026-08-21-literal-replacement-char-anchoring`; `npm run check` green.
