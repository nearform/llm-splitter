# OpenSpec-generated Claude Code integration — regenerated by `openspec update`,
# so leave formatting to the CLI rather than fighting it on every regen.
.claude/

# The published baseline `test/benchmark.js` downloads on first run.
test/.cache/
