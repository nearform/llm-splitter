name: release

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: TODO REWRITE -- add changesets release
        run: echo "TODO REWRITE -- add changesets release"
